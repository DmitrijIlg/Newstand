<div class="article">
  <div class="container">
    <p class="date"><%= @article.created_at.strftime('%b %d') %></p>
    <h2><%= @article.title %></h2>
    <p class="author">by <%= @article.author %></p>
    <p class="article_content"><%= @article.body %></p>

    <!-- Your code here  -->
    <%= current_user.author? %>
		<% if current_user.author? %> 
     <%= link_to "Edit", edit_path(@article.id) %>
    <%= link_to "Delete", destroy_path(@article.id) %>  
		<% end %>
    
  </div>
</div>